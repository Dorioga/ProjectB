import React from "react";

const UpdateSchool = ({ data, options }) => {
  return (
    <div className="border p-4 rounded bg-white">
      <div>UpdateSchool</div>
    </div>
  );
};

export default UpdateSchool;
